<h1><%= current_user.email %></h1>

<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>

<% if @events.present? %>
    <% @events.each do |e| %>
        <li>
            <%= link_to e.name, event_path(e) %>
        </li>
    <% end %>
<% else %>
    <h3>This user has no events at this time. </h3> <br>
<% end %>

<%= link_to "Create Event", new_event_path %>